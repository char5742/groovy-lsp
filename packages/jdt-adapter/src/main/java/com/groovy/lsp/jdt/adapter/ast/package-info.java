/**
 * AST conversion utilities for JDT integration.
 *
 * <p>This package contains utilities for converting between Groovy AST
 * nodes and JDT AST representations, enabling interoperability between
 * the two type systems.</p>
 *
 * <p>This package is null-safe by default. All types are non-null unless
 * explicitly marked with {@code @Nullable}.</p>
 *
 * @since 1.0.0
 */
@org.jspecify.annotations.NullMarked
package com.groovy.lsp.jdt.adapter.ast;
